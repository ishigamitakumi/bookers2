<h2>User info</h2>
   <table class='table'>
    <tr><p>投稿ユーザー画像：<%= image_tag user.get_profile_image("63x63") %></p></tr>
    <tr>
     <th>name</th>
     <th><%= user.name %></th>
    <tr>
     <th>introduction</th>
     <th><%= user.introduction %></th>
    </tr>
   </table>
    <div class='row'>
    <%= link_to edit_user_path(user.id), class: "btn btn-outline-secondary btn-block" do%>
    <i class= "fas fa-user-cog"></i>
    <% end %>
    </div>